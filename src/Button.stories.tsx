import React from 'react'

import Button from '.'

export default {
  title: 'Button',
  component: Button,
}

export const BaseButton = () => {
  return <Button color="black"> Base Button</Button>
}
